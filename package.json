{
  "name": "advent-of-code",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "null",
  "dependencies": {
    "@types/node": "14.14.10",
    "esbuild": "0.8.17",
    "js-combinatorics": "0.5.0",
    "lodash": "4.17.19",
    "md5": "2.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "2019": "for f in 2019/*.js; do echo $f; time node $f; echo ''; done",
    "2020": "for f in 2020/*.?s; do echo $f; time (esbuild $f | node); echo ''; done",
    "2021": "for f in 2021/*.?s; do echo $f; time (esbuild $f | node); echo ''; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
